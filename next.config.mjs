/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['fakestoreapi.com', 'cdn.sanity.io'], // Add Sanity CDN if you're using Sanity images
  },
  
  };
  
  export default nextConfig;
  